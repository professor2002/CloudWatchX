services:
  cloudwatchx:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: cloudwatchx

    ports:
      - "5000:5000"

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    restart: unless-stopped
